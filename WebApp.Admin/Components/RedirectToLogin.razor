@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Ép điều hướng về trang /login
        Navigation.NavigateTo("/management/login", replace: true);
    }
}
